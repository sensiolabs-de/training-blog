{{ form_start(form, { method: 'POST', action: path('blog_comment_new', { 'slug': post.slug }) }) }}
<fieldset>
    <legend>Add a comment</legend>
    {{ form_widget(form) }}
</fieldset>
{{ form_end(form) }}
